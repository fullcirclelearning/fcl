import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div>
      <h1>Cost</h1>
      <p>This is the cost page content.</p>
      {/* More content */}
    </div>
  );
});